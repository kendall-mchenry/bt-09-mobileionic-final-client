import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'final-task-client',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
